# Say That Again

A small swipe-card conversation game. The player gets a phrase to work into conversation, then swipes right when they succeed or left when they miss it. Scores are saved in the browser with `localStorage`.

## Run Locally

Open `docs/index.html` in a browser.

## GitHub Pages

Configure Pages to serve from the `docs/` folder on the main branch.
